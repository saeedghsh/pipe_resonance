name: pipe_resonance

channels:
  - conda-forge

dependencies:
  # core interpreter
  - python=3.11
  # dev & code-quality
  - pylint
  - black
  - mypy
  # science stack
  - pydantic
  - opencv
  - numpy
  - matplotlib
  # pip
  - pip
